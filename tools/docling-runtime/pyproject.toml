[project]
name = "lingopane-docling-runtime"
version = "0.1.0"
description = "Pinned local Docling runtime used by the LingoPane prototype worker"
requires-python = ">=3.12,<3.15"
dependencies = [
  "docling[ocrmac]==2.113.0",
]

[tool.uv]
package = false
